export const metadata = {
  title: "Potomac CMS",
  description: "Content management studio for Potomac",
};

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
